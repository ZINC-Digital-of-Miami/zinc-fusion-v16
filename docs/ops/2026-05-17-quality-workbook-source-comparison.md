# 2026-05-17 Quality Workbook Source Comparison

Status: source comparison and V16 tooling update record

## Sources Compared

- Warbird V9 repo: `/Users/zincdigital/mnt/SatechiHub/warbird-pro`
- Upstream Quality Playbook checkout:
  `/Users/zincdigital/.codex/tool-sources/quality-playbook`
- V16 install: `/Users/zincdigital/mnt/SatechiHub/ZINC-FUSION-V16`

## Findings

- Warbird V9's installed `quality-playbook` skill is older (`v1.2.0`) than
  V16's prior install (`v1.5.6`) and upstream's current comparison target
  (`v1.5.7`).
- Warbird V9 has useful project-specific guard lanes, especially
  `scripts/guards/check-local-quality-lane.sh` and
  `scripts/guards/warbird-agent-precheck.sh`, but those are Warbird-specific
  Pine/V9 gates. They are not copied into V16.
- Upstream `v1.5.7` adds critical bundle-completeness fixes missing from V16:
  `bin/reference_docs_ingest.py`, `bin/benchmark_lib.py`, externalized phase
  guide references, and a bundle-presence smoke check in the installer.
- V16 already had the broader upstream `v1.5.6` bundle structure, but its
  wrapper doctor did not require the v1.5.7 Phase 1 support modules.

## Locked Update

V16 is now pinned to upstream Quality Playbook
`ce3a90b14631ccea2c790945e4f6dc9717f6a444` (`v1.5.7`). The wrapper's required
bundle list now includes the v1.5.7 critical files, and the repo has
`reference_docs/` sentinels for future citable documentation intake.

The V16 wrapper now bounds installed-gate execution with
`quality_gate_timeout_seconds` so a stuck gate is reported as a failed check
instead of blocking the workflow indefinitely.

The repo also restores empty `.yarn` sentinel directories required by upstream
v1.5.7's pre-run sentinel check:
`.yarn/patches`, `.yarn/plugins`, `.yarn/releases`, and `.yarn/versions`.
The `.gitignore` exceptions now name the concrete `.gitkeep` files so the
upstream sentinel parser treats them as file sentinels instead of missing
directory paths.

Generated `quality/` workbook artifacts were not regenerated by this comparison.
Until a new approved Quality Playbook artifact run completes, existing generated
artifacts remain historical evidence, not proof that the current source lane is
fully fresh.

## Verification

- `python3 -m unittest discover -s scripts/tests -p 'test_*.py'`: PASS
- `python3 scripts/qplaybook.py doctor`: PASS
- `python3 scripts/qplaybook.py smoke --profile code --no-llm`: PASS
- `bash quality/mechanical/verify.sh`: FAIL; generated mechanical baselines are
  stale against current source line locations.
- Installed `quality_gate.py`: NOT RUN to completion in this comparison pass; an
  attempted gate run stopped producing terminal output and was terminated. The
  wrapper timeout was added after that observation.
- First v1.5.7 regeneration attempt: FAIL before Phase 1 because `.yarn`
  sentinel directories were absent; sentinel `.gitkeep` files were restored.
- Second v1.5.7 regeneration attempt: FAIL before Phase 1 because `.gitignore`
  unignored `.yarn` directories without concrete `.gitkeep` file exceptions;
  the `.gitignore` sentinel rules were corrected.
