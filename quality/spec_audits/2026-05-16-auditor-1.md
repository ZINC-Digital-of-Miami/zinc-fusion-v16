### app/api/zl/intraday/route.ts
- **Line 16:** [MISSING] [REQ-001] Route comment promises `15m -> 1m` fallback, but code executes only the 15m query path.
  Spec says: `quality/REQUIREMENTS.md` REQ-001 requires executable fallback to `mkt.price_1m` and source metadata that reflects the table actually used.
  Code does: lines `17-23` query only `mkt.price_15m`; line `45` hardcodes `source: "mkt.price_15m"`.

### app/api/zl/forecast-targets/route.ts
- **Line 60:** [DIVERGENT] [REQ-002] Endpoint still emits route-specific envelope fields rather than canonical `ApiEnvelope` keys.
  Spec says: REQ-002 requires equivalent forecast-target surfaces to share one stable envelope or one explicit typed adapter path.
  Code does: lines `60-63` return `{ error, asOfDate, targets }`; lines `103-106` return `{ asOfDate, targets }`.

### components/chart/ZlCandlestickChart.tsx
- **Line 40:** [MISSING] [REQ-002] Target-zone overlay is globally disabled even when `targetZones` are provided.
  Spec says: `quality/CONTRACTS.md` (`C-006`, `C-007`) and `quality/COVERAGE_MATRIX.md` map REQ-002 to visible target-zone projection when data exists.
  Code does: line `40` sets `SHOW_FORECAST_TARGET_OVERLAY = false`; attach/render branches at lines `260-269` never execute.

### components/dashboard/ProbabilitySurface.tsx
- **Line 62:** [DIVERGENT] [REQ-002] Probability surface is still tied to the divergent `/api/zl/forecast-targets` payload contract.
  Spec says: REQ-002 requires one canonical envelope contract (or one explicit adapter) across equivalent forecast-target consumers.
  Code does: line `62` fetches `/api/zl/forecast-targets`; lines `67-68` parse `json.targets`; line `82` reads `json.asOfDate`.

### lib/server/ingest-run.ts + SQL writers
- **Line 3 (TS) / 51, 91, 139 (SQL) / 56, 365, 382 (SQL):** [DIVERGENT] [REQ-003] `ops.ingest_run.status` vocabulary remains split across writers.
  Spec says: REQ-003 requires one canonical writer vocabulary.
  Code does: TS writes `RUNNING/SUCCESS/FAILED`; SQL writers still write lowercase/alternate values (`running/ok/failed/error`).

### README.md
- **Line 95:** [DIVERGENT] [REQ-006] Root README still hardcodes `localhost:3000` startup guidance.
  Spec says: REQ-006 prohibits hardcoded port assumptions and requires alignment with authority rules.
  Code does: line `95` explicitly instructs users that the app "should now be running on localhost:3000".
