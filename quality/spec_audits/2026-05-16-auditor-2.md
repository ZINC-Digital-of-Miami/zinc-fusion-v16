### app/api/zl/intraday/route.ts
- **Line 16:** [DIVERGENT] [REQ-001] Behavior does not match declared fallback architecture.
  Spec says: REQ-001 demands executable fallback parity between declaration and implementation.
  Code does: only `price_15m` query path exists; no `price_1m` branch is present.

### app/api/zl/forecast-targets/route.ts + components/dashboard/ProbabilitySurface.tsx
- **Line 103 (route) / 62 (consumer):** [PHANTOM] [REQ-002] Equivalent data surface is implemented as a separate ad-hoc contract and directly consumed by one client.
  Spec says: REQ-002 allows divergence only with one explicit adapter contract consumed consistently.
  Code does: route returns `{asOfDate, targets}`; probability surface parses this shape directly (`json.targets`, `json.asOfDate`), while other clients consume `ApiEnvelope` from `/api/zl/target-zones`.

### components/chart/ZlCandlestickChart.tsx
- **Line 40:** [MISSING] [REQ-002] Forecast target primitive lifecycle exists but runtime gate is hard-disabled.
  Spec says: REQ-002 + contract mapping requires visible target-zone projection when target data is supplied.
  Code does: constant gate is false, so attach/detach logic is effectively dead.

### lib/server/ingest-run.ts + SQL migrations
- **Line 3 (TS) / 139 (`20260414001_ingest_zl_intraday.sql`) / 365 (`202605080003_supabase_trusted_site_fill_hourly.sql`):** [DIVERGENT] [REQ-003] Mixed status vocabulary remains unresolved.
  Spec says: REQ-003 requires one canonical status set across all writers.
  Code does: TS and SQL writers still emit incompatible literal sets.

### README.md
- **Line 95:** [DIVERGENT] [REQ-006] Operator documentation still violates no-hardcoded-port rule.
  Spec says: REQ-006 requires startup instructions without fixed localhost port assumptions.
  Code does: README still prescribes `localhost:3000` directly.

### app/api/strategy/posture/route.ts (follow-up hypothesis)
- **Line 299:** [UNDOCUMENTED] [REQ-005] Hypothesis: degraded runtime-pull state may not be machine-typed at the envelope boundary.
  Spec says: REQ-005 requires fallback metadata sufficient to identify degraded mode.
  Code does: response includes `ai` metadata and card-level provenance, but no explicit `degraded` boolean on the top-level envelope.
  Auditor note: mark as **Needs verification**; confirm whether existing `ai` metadata satisfies REQ-005 before classifying as defect.
