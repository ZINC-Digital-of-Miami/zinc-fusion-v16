### components/chart/ZlCandlestickChart.tsx
- **Line 40:** [MISSING] [REQ-002] Intentional-looking kill-switch disables forecast target overlays globally.
  Spec says: REQ-002 requires target-zone contract parity across consumer surfaces; hidden overlays invalidate parity even when data exists.
  Code does: `SHOW_FORECAST_TARGET_OVERLAY = false` blocks primitive attachment and projected target-zone rendering.

### components/dashboard/ProbabilitySurface.tsx
- **Line 62:** [DIVERGENT] [REQ-002] Consumer depends on a route-specific payload shape instead of the canonical envelope path.
  Spec says: REQ-002 requires one stable envelope contract or one shared adapter.
  Code does: fetches `/api/zl/forecast-targets`, then directly consumes `json.targets/json.asOfDate`.

### app/api/zl/forecast-targets/route.ts
- **Line 60 / 103:** [DIVERGENT] [REQ-002] Endpoint keeps bespoke response shape and keeps client divergence alive.
  Spec says: REQ-002 requires convergence of equivalent semantic endpoints.
  Code does: returns bespoke shape in both success and error paths, not `ApiEnvelope`.

### app/api/zl/intraday/route.ts
- **Line 16 / 45:** [MISSING] [REQ-001] Fallback semantics and source metadata are internally inconsistent.
  Spec says: REQ-001 requires executable fallback and truthful source labeling.
  Code does: no 1m query branch exists; source is pinned to `mkt.price_15m`.

### lib/server/ingest-run.ts and SQL writers
- **Line 3 (TS) with SQL writers at `20260414001...:139`, `202605080003...:56,365,382`:** [DIVERGENT] [REQ-003] Status taxonomy mismatch creates cross-writer analytics ambiguity.
  Spec says: REQ-003 requires one canonical writer vocabulary.
  Code does: uppercase TS values and lowercase/alternate SQL values coexist.

### README.md
- **Line 95:** [DIVERGENT] [REQ-006] Documentation still contradicts V16 authority startup constraints.
  Spec says: REQ-006 bans hardcoded localhost port assumptions.
  Code does: README still contains `localhost:3000` startup instruction.

### app/api/sentiment/overview/route.ts (follow-up hypothesis)
- **Line 370:** [UNDOCUMENTED] [REQ-005] Hypothesis: degraded runtime-pull signaling may depend on prose rather than a typed field.
  Spec says: REQ-005 requires fallback metadata capable of identifying degraded mode.
  Code does: route returns `ai` metadata and provenance-rich card payload; explicit `degraded` flag is absent.
  Auditor note: requires verification probe before elevating to bug.
