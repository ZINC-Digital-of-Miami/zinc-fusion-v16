## Summary
The root onboarding README hardcodes `localhost:3000`, conflicting with the documented no-hardcoded-port startup rule.

## Spec reference
- Requirement: REQ-006
- Spec basis: REQ-006 requires operator docs to avoid startup guidance that conflicts with V16 authority rules.
- Behavioral contract quote: "avoid startup guidance that conflicts with V16 authority rules"

## The code
At `README.md:95`, the startup guidance states that the app should be running on `localhost:3000`, while authority rules at `AGENTS.md:53` explicitly prohibit hardcoded port assumptions.

## Observable consequence
Searching the root README for `localhost:3000` reproduces the violation. Expected behavior is dynamic port discovery guidance; actual behavior instructs operators to assume a fixed port.

## The fix
```diff
--- a/README.md
+++ b/README.md
@@
-The starter kit should now be running on [localhost:3000](http://localhost:3000/).
+The development server starts on the first available local port. Use the terminal output URL instead of assuming port 3000.
```

## The test
- Regression test: quality/tests/test_bug_004_readme_port_rule.py::test_bug_004_readme_must_not_hardcode_localhost_3000
- Regression patch: quality/patches/BUG-004-regression-test.patch
- Fix patch: quality/patches/BUG-004-fix.patch
- Red receipt: quality/results/BUG-004.red.log
- Green receipt: quality/results/BUG-004.green.log
