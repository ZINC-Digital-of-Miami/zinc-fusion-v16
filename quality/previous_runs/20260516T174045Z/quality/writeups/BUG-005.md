# Bug Writeup
> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.6 — Andrew Stellman
> Date: 2026-05-16 · Project: ZINC-FUSION-V16

## Summary
Intraday route fallback claim diverges from implementation in `app/api/zl/intraday/route.ts:10-16` causes this observable failure: comment claims fallback; only `mkt.price_15m` query is implemented.

## Spec reference
- Requirement: REQ-003
- Spec basis: REQ-003 conditions 1-2 require fallback behavior and route contract text to match.
- Behavioral contract quote: "behavior, comments, and consumer assumptions MUST agree on whether 1m fallback exists."

## The code
At `app/api/zl/intraday/route.ts:10-16`, the current implementation behavior is: comment claims fallback; only `mkt.price_15m` query is implemented.

## Observable consequence
Expected behavior: either query fallback to `mkt.price_1m` exists or fallback claim is removed. Actual behavior: comment claims fallback; only `mkt.price_15m` query is implemented.

## The fix
```diff
--- a/app/api/zl/intraday/route.ts
+++ b/app/api/zl/intraday/route.ts
@@ -7,14 +7,34 @@ export async function GET() {
   try {
     const supabase = createSupabaseAdminClient();
 
-    // Try 15-minute bars first, fall back to 1-minute
-    const { data: rows, error } = await supabase
+    // Try 15-minute bars first, then explicitly fall back to 1-minute.
+    let effectiveSource = "mkt.price_15m";
+    const { data: rows15m, error } = await supabase
       .schema("mkt")
       .from("price_15m")
       .select("symbol, bucket_ts, open, high, low, close, volume")
       .eq("symbol", "ZL")
       .order("bucket_ts", { ascending: true });
+
+    let rows = rows15m;
+
+    if (!error && (!rows15m || rows15m.length === 0)) {
+      const { data: rows1m, error: fallbackError } = await supabase
+        .schema("mkt")
+        .from("price_1m")
+        .select("symbol, bucket_ts, open, high, low, close, volume")
+        .eq("symbol", "ZL")
+        .order("bucket_ts", { ascending: true });
+
+      if (fallbackError) {
+        return NextResponse.json(
+          { ok: false, data: [], asOf: new Date().toISOString(), error: fallbackError.message },
+          { status: 500 },
+        );
+      }
+
+      rows = rows1m;
+      effectiveSource = "mkt.price_1m";
+    }
 
     if (error) {
       return NextResponse.json(
@@ -36,7 +56,7 @@ export async function GET() {
       ok: true,
       data: bars,
       asOf: new Date().toISOString(),
-      source: "mkt.price_15m",
+      source: effectiveSource,
     };
 
     return NextResponse.json(envelope);
```

## The test
- Regression test: quality/test_regression.py::test_bug_005_intraday_fallback_claim_matches_implemented_query_path
- Regression patch: quality/patches/BUG-005-regression-test.patch
- Fix patch: quality/patches/BUG-005-fix.patch
- Red receipt: quality/results/BUG-005.red.log
- Green receipt: quality/results/BUG-005.green.log
