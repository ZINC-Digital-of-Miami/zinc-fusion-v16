# Bug Writeup
> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.6 — Andrew Stellman
> Date: 2026-05-16 · Project: ZINC-FUSION-V16

## Summary
`ops.ingest_run` status vocabulary is split across writers in `lib/server/ingest-run.ts:3,17,38,58`; `supabase/migrations/20260414001_ingest_zl_intraday.sql:51,91,139,219`; `supabase/migrations/202605080003_supabase_trusted_site_fill_hourly.sql:56,365,382` causes this observable failure: mixed uppercase/lowercase and divergent terminals (`SUCCESS`, `ok`, `error`, `FAILED`, `failed`).

## Spec reference
- Requirement: REQ-001
- Spec basis: REQ-001 condition 3 requires one canonical status vocabulary.
- Behavioral contract quote: "MUST include stable run_id lifecycle semantics and one canonical status vocabulary."

## The code
At `lib/server/ingest-run.ts:3,17,38,58`; `supabase/migrations/20260414001_ingest_zl_intraday.sql:51,91,139,219`; `supabase/migrations/202605080003_supabase_trusted_site_fill_hourly.sql:56,365,382`, the current implementation behavior is: mixed uppercase/lowercase and divergent terminals (`SUCCESS`, `ok`, `error`, `FAILED`, `failed`).

## Observable consequence
Expected behavior: all writers emit one normalized status set. Actual behavior: mixed uppercase/lowercase and divergent terminals (`SUCCESS`, `ok`, `error`, `FAILED`, `failed`).

## The fix
```diff
--- a/lib/server/ingest-run.ts
+++ b/lib/server/ingest-run.ts
@@ -1,6 +1,6 @@
 import type { SupabaseClient } from "@supabase/supabase-js";
 
-export type IngestRunStatus = "RUNNING" | "SUCCESS" | "FAILED";
+export type IngestRunStatus = "running" | "success" | "failed";
@@ -14,7 +14,7 @@ export async function logIngestRunStart(
       run_id: runId,
       job_name: jobName,
       source,
-      status: "RUNNING",
+      status: "running",
       started_at: new Date().toISOString(),
       records_upserted: 0,
     });
@@ -35,7 +35,7 @@ export async function logIngestRunFinish(
       .schema("ops")
       .from("ingest_run")
       .update({
-        status: "SUCCESS",
+        status: "success",
         finished_at: new Date().toISOString(),
         records_upserted: recordsUpserted,
       })
@@ -55,7 +55,7 @@ export async function logIngestRunFailure(
       .schema("ops")
       .from("ingest_run")
       .update({
-        status: "FAILED",
+        status: "failed",
         finished_at: new Date().toISOString(),
         error_message: errorMessage.slice(0, 2000),
       })
--- a/supabase/migrations/202605080003_supabase_trusted_site_fill_hourly.sql
+++ b/supabase/migrations/202605080003_supabase_trusted_site_fill_hourly.sql
@@ -362,7 +362,7 @@ BEGIN
 
   UPDATE ops.ingest_run
-  SET status = 'ok',
+  SET status = 'success',
       finished_at = now(),
       records_upserted = v_records_upserted,
       error_message = NULL,
@@ -379,7 +379,7 @@ EXCEPTION WHEN OTHERS THEN
   UPDATE ops.ingest_run
-  SET status = 'error',
+  SET status = 'failed',
       finished_at = now(),
       error_message = left(SQLERRM, 1000),
       ingested_at = now()
--- a/supabase/migrations/20260414001_ingest_zl_intraday.sql
+++ b/supabase/migrations/20260414001_ingest_zl_intraday.sql
@@ -138,7 +138,7 @@ BEGIN
   -- 5. Log success
   INSERT INTO ops.ingest_run (job_name, source, started_at, finished_at, status, records_upserted, error_message)
-  VALUES ('ingest_zl_intraday', 'databento', started, now(), 'ok', records_in, NULL);
+  VALUES ('ingest_zl_intraday', 'databento', started, now(), 'success', records_in, NULL);
@@ -218,7 +218,7 @@ BEGIN
   -- 3. Log success
   INSERT INTO ops.ingest_run (job_name, source, started_at, finished_at, status, records_upserted, error_message)
-  VALUES ('rollup_zl_daily', 'rollup', started, now(), 'ok', records_out, NULL);
+  VALUES ('rollup_zl_daily', 'rollup', started, now(), 'success', records_out, NULL);
 EOF
```

## The test
- Regression test: quality/test_regression.py::test_bug_002_ingest_status_vocabulary_is_canonical_across_writers
- Regression patch: quality/patches/BUG-002-regression-test.patch
- Fix patch: quality/patches/BUG-002-fix.patch
- Red receipt: quality/results/BUG-002.red.log
- Green receipt: quality/results/BUG-002.green.log

## Related issues
BUG-001, BUG-010
