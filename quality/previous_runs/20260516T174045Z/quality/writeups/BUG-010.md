# Bug Writeup
> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.6 — Andrew Stellman
> Date: 2026-05-16 · Project: ZINC-FUSION-V16

## Summary
Trusted-site-fill failure path does not close the active `run_id` in `scripts/fill_site_with_trusted_data.py:1816-1818,1848-1853` causes this observable failure: success path updates `WHERE run_id = %s`, but failure path updates the latest row by `job_name` subquery.

## Spec reference
- Requirement: REQ-001
- Spec basis: REQ-001 condition 2 requires terminal status updates to close the same run identifier opened at run start.
- Behavioral contract quote: "MUST include stable run_id lifecycle semantics and one canonical status vocabulary."

## The code
At `scripts/fill_site_with_trusted_data.py:1816-1818,1848-1853`, the current implementation behavior is: success path updates `WHERE run_id = %s`, but failure path updates the latest row by `job_name` subquery.

## Observable consequence
Expected behavior: both success and failure paths update `ops.ingest_run` using the local `run_id` created at start. Actual behavior: success path updates `WHERE run_id = %s`, but failure path updates the latest row by `job_name` subquery.

## The fix
```diff
--- a/scripts/fill_site_with_trusted_data.py
+++ b/scripts/fill_site_with_trusted_data.py
@@ -1845,14 +1845,9 @@ def main() -> None:
                 SET status = 'error',
                     finished_at = %s,
                     error_message = %s
-                WHERE run_id = (
-                  SELECT run_id FROM ops.ingest_run
-                  WHERE job_name = 'trusted_site_fill'
-                  ORDER BY started_at DESC
-                  LIMIT 1
-                )
+                WHERE run_id = %s
                 """,
-                (datetime.now(timezone.utc), str(err)[:1000]),
+                (datetime.now(timezone.utc), str(err)[:1000], str(run_id)),
             )
             conn.commit()
         except Exception:  # noqa: BLE001
```

## The test
- Regression test: quality/test_regression.py::test_bug_010_trusted_site_fill_failure_updates_same_run_id
- Regression patch: quality/patches/BUG-010-regression-test.patch
- Fix patch: quality/patches/BUG-010-fix.patch
- Red receipt: quality/results/BUG-010.red.log
- Green receipt: quality/results/BUG-010.green.log

## Related issues
BUG-001, BUG-002
