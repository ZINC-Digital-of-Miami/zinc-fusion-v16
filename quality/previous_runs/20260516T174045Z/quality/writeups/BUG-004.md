# Bug Writeup
> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.6 — Andrew Stellman
> Date: 2026-05-16 · Project: ZINC-FUSION-V16

## Summary
Gate 3 enforces deprecated `/api/cron` route checks in `scripts/verify/gate3.sh:5` causes this observable failure: gate fails unless `app/api/cron` and `runCronHandler` exist.

## Spec reference
- Requirement: REQ-002
- Spec basis: REQ-002 conditions 2-3 require gate policy alignment with pg_cron-only architecture.
- Behavioral contract quote: "MUST treat pg_cron-only architecture as authoritative and MUST NOT enforce /api/cron."

## The code
At `scripts/verify/gate3.sh:5`, the current implementation behavior is: gate fails unless `app/api/cron` and `runCronHandler` exist.

## Observable consequence
Expected behavior: gate verifies architecture compliance without `/api/cron` route dependency. Actual behavior: gate fails unless `app/api/cron` and `runCronHandler` exist.

## The fix
```diff
--- a/scripts/verify/gate3.sh
+++ b/scripts/verify/gate3.sh
@@ -2,8 +2,8 @@
 set -euo pipefail
 
 echo "[Gate3] Auth/security scaffold checks"
-rg -n "runCronHandler" app/api/cron -g 'route.ts' >/dev/null
 rg -n "SUPABASE_SERVICE_ROLE_KEY" lib/server/supabase-admin.ts >/dev/null
 rg -n "startsWith\(\"/api/\"\)" lib/supabase/proxy.ts >/dev/null
+! rg -n "app/api/cron|runCronHandler|/api/cron" -g '!quality/**' . >/dev/null
 
 BASE_URL="${BASE_URL:-}"
 if [ -n "${BASE_URL}" ]; then
```

## The test
- Regression test: quality/test_regression.py::test_bug_004_gate3_does_not_require_api_cron_handler_files
- Regression patch: quality/patches/BUG-004-regression-test.patch
- Fix patch: quality/patches/BUG-004-fix.patch
- Red receipt: quality/results/BUG-004.red.log
- Green receipt: quality/results/BUG-004.green.log

## Related issues
BUG-003
