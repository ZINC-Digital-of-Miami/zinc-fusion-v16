# Bug Writeup
> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.6 — Andrew Stellman
> Date: 2026-05-16 · Project: ZINC-FUSION-V16

## Summary
Risk-factor API labels oldest component date as canonical freshness in `app/api/dashboard/risk-factors/route.ts:715-718` causes this observable failure: `as_of_date` is explicitly set to `asOfDateMin`.

## Spec reference
- Requirement: REQ-005
- Spec basis: REQ-005 condition 3 requires freshness labels to avoid implying full-vector recency from oldest subset timestamp.
- Behavioral contract quote: "Freshness labels do not imply full-vector recency when only subset is stale."

## The code
At `app/api/dashboard/risk-factors/route.ts:715-718`, the current implementation behavior is: `as_of_date` is explicitly set to `asOfDateMin`.

## Observable consequence
Expected behavior: canonical freshness reflects the intended global freshness semantics, not forced oldest-date assignment. Actual behavior: `as_of_date` is explicitly set to `asOfDateMin`.

## The fix
```diff
--- a/app/api/dashboard/risk-factors/route.ts
+++ b/app/api/dashboard/risk-factors/route.ts
@@ -714,7 +714,7 @@ export async function GET() {
       .sort();
     const asOfDateMin = allDates[0] ?? null;
     const asOfDateMax = allDates[allDates.length - 1] ?? null;
-    const asOfDate = asOfDateMin;
+    const asOfDate = asOfDateMax ?? asOfDateMin;
     const mixedVintage = Boolean(asOfDateMin && asOfDateMax && asOfDateMin !== asOfDateMax);
 
     const mergeDriver = (
```

## The test
- Regression test: quality/test_regression.py::test_bug_008_risk_factors_do_not_use_oldest_component_as_canonical_as_of_date
- Regression patch: quality/patches/BUG-008-regression-test.patch
- Fix patch: quality/patches/BUG-008-fix.patch
- Red receipt: quality/results/BUG-008.red.log
- Green receipt: quality/results/BUG-008.green.log
