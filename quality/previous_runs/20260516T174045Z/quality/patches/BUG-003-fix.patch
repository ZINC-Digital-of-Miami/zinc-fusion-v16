--- a/lib/supabase/proxy.ts
+++ b/lib/supabase/proxy.ts
@@ -12,10 +12,6 @@ function isPublicPath(pathname: string): boolean {
   return false;
 }
 
-function isCronPath(pathname: string): boolean {
-  return pathname.startsWith("/api/cron/");
-}
-
 function buildLoginRedirect(request: NextRequest): NextResponse {
   const url = request.nextUrl.clone();
   const next = `${request.nextUrl.pathname}${request.nextUrl.search}`;
@@ -36,11 +32,6 @@ export async function updateSession(request: NextRequest) {
     return supabaseResponse;
   }
 
-  // Cron routes are machine-to-machine; they enforce CRON_SECRET per route.
-  if (isCronPath(pathname)) {
-    return supabaseResponse;
-  }
-
   // Fail closed if auth env is missing for protected surfaces.
   if (!hasEnvVars) {
     if (pathname.startsWith("/api/")) {
