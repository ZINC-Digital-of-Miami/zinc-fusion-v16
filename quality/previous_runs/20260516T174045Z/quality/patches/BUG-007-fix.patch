--- a/app/api/zl/forecast-targets/route.ts
+++ b/app/api/zl/forecast-targets/route.ts
@@ -1,6 +1,7 @@
 import { NextResponse } from "next/server";
 
 import { createSupabaseAdminClient } from "@/lib/server/supabase-admin";
+import type { ApiEnvelope } from "@/lib/contracts/api";
 
 type ForecastTarget = {
   id: string;
@@ -11,6 +12,11 @@ type ForecastTarget = {
   coveragePct: number | null;
 };
+
+type ForecastTargetsPayload = {
+  asOfDate: string | null;
+  targets: ForecastTarget[];
+};
@@ -53,12 +59,17 @@ export async function GET() {
 
     if (error) {
+      const envelope: ApiEnvelope<ForecastTargetsPayload> = {
+        ok: false,
+        data: { asOfDate: null, targets: [] },
+        asOf: new Date().toISOString(),
+        source: "forecasts.target_zones",
+      };
       return NextResponse.json(
-        { error: error.message, asOfDate: null, targets: [] as ForecastTarget[] },
+        { ...envelope, error: error.message },
         { status: 500 },
       );
     }
@@ -60,10 +71,15 @@ export async function GET() {
       return NextResponse.json({
-        asOfDate: null,
-        targets: [] as ForecastTarget[],
+        ok: true,
+        data: { asOfDate: null, targets: [] as ForecastTarget[] },
+        asOf: new Date().toISOString(),
+        source: "forecasts.target_zones",
       });
     }
@@ -97,9 +113,12 @@ export async function GET() {
-    return NextResponse.json({
-      asOfDate,
-      targets,
-    });
+    const envelope: ApiEnvelope<ForecastTargetsPayload> = {
+      ok: true,
+      data: { asOfDate, targets },
+      asOf: new Date().toISOString(),
+      source: "forecasts.target_zones",
+    };
+    return NextResponse.json(envelope);
   } catch (err) {
     return NextResponse.json(
-      { error: String(err), asOfDate: null, targets: [] as ForecastTarget[] },
+      { ok: false, data: { asOfDate: null, targets: [] as ForecastTarget[] }, asOf: new Date().toISOString(), source: "forecasts.target_zones", error: String(err) },
       { status: 500 },
     );
   }
