### supabase/migrations/202605080001_fix_zl_ingest_freshness.sql
- **Line 39:** [MISSING] [REQ-001] Latest `ingest_zl_intraday()` does not maintain one `run_id` lifecycle row.
  Spec says: `quality/REQUIREMENTS.md:118-123` requires stable `run_id`, start and terminal state for the same run, canonical status, and error detail.
  Code does: inserts failure/success rows without `run_id` at lines 39, 119, and 163; no start row or terminal `UPDATE ... WHERE run_id`.

### supabase/migrations/20260414001_ingest_zl_intraday.sql
- **Line 218:** [MISSING] [REQ-001] `rollup_zl_daily()` logs only an uncorrelated terminal row.
  Spec says: `quality/REQUIREMENTS.md:120-124` requires writer inserts to honor `run_id` and log terminal status with timestamps/error detail.
  Code does: inserts into `ops.ingest_run` without `run_id`; no lifecycle start row and no failure terminal path for this function.

### lib/server/ingest-run.ts
- **Line 3:** [DIVERGENT] [REQ-001] TypeScript ingest statuses diverge from SQL status vocabulary.
  Spec says: `quality/REQUIREMENTS.md:118-123` requires one canonical status vocabulary.
  Code does: allows/writes `RUNNING`, `SUCCESS`, `FAILED` at lines 3, 17, 38, 58 while SQL writers use `running`, `ok`, `error`.

### supabase/migrations/202603180011_pg_cron.sql
- **Line 12:** [DIVERGENT] [REQ-001] stale-run cleanup cannot consistently close Phase 4 SQL runs.
  Spec says: `quality/REQUIREMENTS.md:121-124` requires deterministic terminal transitions.
  Code does: sets `TIMEOUT` only where status is `RUNNING` at lines 12-14, but Phase 4 SQL writer starts with lowercase `running`.

### lib/supabase/proxy.ts
- **Line 15:** [PHANTOM] [REQ-002] middleware preserves a forbidden `/api/cron` auth bypass.
  Spec says: `quality/REQUIREMENTS.md:134-137` and `AGENTS.md:149-160` require pg_cron-only architecture with no `/api/cron` or `CRON_SECRET` route surface.
  Code does: matches `/api/cron/`, then lines 39-41 bypass auth assuming per-route cron secret checks.

### scripts/verify/gate3.sh
- **Line 5:** [PHANTOM] [REQ-002] Gate 3 requires a deprecated cron route implementation.
  Spec says: `quality/REQUIREMENTS.md:134-138` says verification must not enforce `/api/cron` route presence.
  Code does: fails unless `runCronHandler` exists under `app/api/cron`.

### app/api/zl/intraday/route.ts
- **Line 10:** [PHANTOM] [REQ-003] documented 15m-to-1m fallback is not implemented.
  Spec says: `quality/REQUIREMENTS.md:148-152` requires an explicit `mkt.price_1m` query path if fallback is claimed.
  Code does: queries only `mkt.price_15m` at lines 11-16 and reports source `mkt.price_15m` at line 39; no `price_1m` path exists in the route.

### scripts/verify/gate6.sh
- **Line 79:** [DIVERGENT] [REQ-004] parity gate treats optional `warning` as required.
  Spec says: `quality/REQUIREMENTS.md:163-167` requires one envelope policy and optional fields optional everywhere.
  Code does: requires `warning` at lines 79, 99, 120, 138, 154, and 170 while `lib/contracts/api.ts:8` declares `warning?: string`.

### app/api/zl/forecast-targets/route.ts
- **Line 97:** [UNDOCUMENTED] [REQ-004] forecast-targets uses an alternate envelope not declared in the shared contract.
  Spec says: `quality/REQUIREMENTS.md:163-167` requires shared envelope semantics or documented endpoint exceptions.
  Code does: returns `{ asOfDate, targets }` at lines 97-100 and error `{ error, asOfDate, targets }` at lines 53-56 instead of `ApiEnvelope`.

### app/api/dashboard/risk-factors/route.ts
- **Line 717:** [DIVERGENT] [REQ-005] API still publishes the oldest component date as canonical `as_of_date`.
  Spec says: `quality/REQUIREMENTS.md:171-182` requires mixed-vintage state without mislabeling the oldest timestamp as global freshness.
  Code does: sets `asOfDate = asOfDateMin` and publishes it as `as_of_date` at line 816.

- **Line 862:** [DIVERGENT] [REQ-004] no-snapshot fallback can look like a normal AI snapshot source.
  Spec says: `quality/QUALITY.md:99-102` requires fallback behavior to expose reliability downgrade cues.
  Code does: when `aiSnapshot` is null, returns `enabled: false` but source defaults to `weekly-db-plus-ai-snapshot` and `generatedAt` defaults to response time.

### app/api/zl/forecast/route.ts
- **Line 62:** [MISSING] [REQ-006] unknown horizons are silently dropped without logging.
  Spec says: `quality/REQUIREMENTS.md:193-197` requires unknown horizons to be filtered and logged consistently.
  Code does: `if (!horizonDays) continue;`; the same silent drop exists in `app/api/zl/target-zones/route.ts:62` and `app/api/zl/forecast-targets/route.ts:72`.

### lib/server/ai-snapshot.ts
- **Line 49:** [MISSING] [REQ-004] snapshot whitelist rejection has no observable reason.
  Spec says: `quality/QUALITY.md:99-102` requires whitelist changes to be deliberate and fallback behavior to expose downgrade cues.
  Code does: returns `null` on untrusted source; lines 70-71 also swallow load/parse errors as `null`, making source drift, missing file, and invalid JSON indistinguishable.