### supabase/migrations/202605080001_fix_zl_ingest_freshness.sql
- **Line 39:** [MISSING] [REQ-001] Active intraday writer still inserts `ops.ingest_run` rows without `run_id` lifecycle ownership.
  Spec says: `quality/REQUIREMENTS.md` REQ-001 conditions 1-2 require stable `run_id` and same-run start/terminal transitions.
  Code does: lines 39-40, 119-121, and 163-164 write terminal rows with no `run_id`, so rows cannot be lifecycle-correlated.

### scripts/fill_site_with_trusted_data.py
- **Line 1848:** [DIVERGENT] [REQ-001] Exception path updates latest row by `job_name` instead of the in-scope `run_id`.
  Spec says: REQ-001 condition 2 requires start and terminal state for the same `run_id`.
  Code does: success path updates `WHERE run_id = %s` at lines 1816-1818, but exception path uses a subquery selecting latest `trusted_site_fill` row at lines 1848-1853.

### supabase/migrations/202603180011_pg_cron.sql
- **Line 12:** [DIVERGENT] [REQ-001] stale-run cleanup uses uppercase status contract inconsistent with lowercase writers.
  Spec says: REQ-001 condition 3 requires one canonical status vocabulary.
  Code does: lines 12-14 set/check `TIMEOUT` / `RUNNING`, while active SQL writers emit lowercase (`running`, `ok`, `error`).

### lib/supabase/proxy.ts
- **Line 15:** [PHANTOM] [REQ-002] middleware includes `/api/cron` bypass despite pg_cron-only architecture.
  Spec says: REQ-002 and `AGENTS.md` architecture rules prohibit `/api/cron` route dependency.
  Code does: `isCronPath` at lines 15-16 and early return at lines 39-41 preserve machine-route bypass semantics.

### scripts/verify/gate3.sh
- **Line 5:** [PHANTOM] [REQ-002] Gate 3 requires cron handler symbols under `app/api/cron`.
  Spec says: REQ-002 conditions 2-3 require verification scripts to align to pg_cron-only architecture.
  Code does: line 5 hard-fails if `runCronHandler` is absent in `app/api/cron`.

### app/api/zl/intraday/route.ts
- **Line 10:** [DIVERGENT] [REQ-003] fallback comment promises `15m -> 1m` behavior not implemented.
  Spec says: REQ-003 conditions 1-2 require fallback claim and executed query path to match.
  Code does: lines 11-16 query only `price_15m`; response source is always `mkt.price_15m` at line 39.

### scripts/verify/gate6.sh
- **Line 79:** [DIVERGENT] [REQ-004] parity harness requires `warning` where contract declares it optional.
  Spec says: REQ-004 condition 1 requires optional fields to stay optional across contract/routes/gates.
  Code does: lines 79, 99, 120, 138, 154, 170 assert required `warning`, while `lib/contracts/api.ts:8` declares `warning?: string`.

### app/api/zl/forecast-targets/route.ts
- **Line 97:** [UNDOCUMENTED] [REQ-004] endpoint uses non-`ApiEnvelope` response and is outside Gate 6 parity coverage.
  Spec says: REQ-004 conditions 2-3 require shared envelope families or documented parity-tested exceptions.
  Code does: lines 97-100 and 54-57 return `{asOfDate, targets}` / `{error, asOfDate, targets}`; `scripts/verify/gate6.sh` does not verify this endpoint.

### app/api/dashboard/risk-factors/route.ts
- **Line 717:** [DIVERGENT] [REQ-005] canonical `as_of_date` is assigned to `as_of_date_min`.
  Spec says: REQ-005 condition 3 prohibits implying full-vector recency from oldest component date.
  Code does: lines 715-718 set `asOfDate = asOfDateMin`; response exports `as_of_date` at line 816.

### app/api/zl/forecast/route.ts
- **Line 62:** [MISSING] [REQ-006] unknown horizons are dropped silently (no diagnostics).
  Spec says: REQ-006 condition 3 requires consistent filtering and logging of unknown horizons.
  Code does: `if (!horizonDays) continue;` in forecast (`line 62`), target-zones (`line 62`), and forecast-targets (`line 72`) drops rows with no logging.
